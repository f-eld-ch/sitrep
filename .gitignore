# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
.idea
.vscode

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Go coverage profiles
coverage.out
# The name the workflows actually pass to -coverprofile. Left untracked it makes the tree
# dirty, and ko refuses to build a dirty tree rather than stamp a commit that does not
# describe what was built.
go-coverage.out
go-cobertura.xml

# Vitest coverage reports
ui/coverage/

# TypeScript incremental build state, emitted for composite projects (tsconfig.node.json)
*.tsbuildinfo
